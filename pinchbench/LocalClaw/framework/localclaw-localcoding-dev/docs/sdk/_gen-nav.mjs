// 统一注入「面包屑 + 折叠全局目录」导航块到 docs/sdk/*.md 顶部。
// 唯一数据源：下方 top / modules。改标题或加章节只动这里，然后 `node _gen-nav.mjs`。
import { readFileSync, writeFileSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const DIR = dirname(fileURLToPath(import.meta.url));

// 顶级章节（README 为首页，不在此列也不注入）
const top = [
  ['01-overview.md', '1', '文档总览'],
  ['02-getting-started.md', '2', '快速上手'],
  ['03-core-capabilities.md', '3', '核心能力详解'],
  ['04-examples.md', '4', '代码示例专区'],
  ['05-configuration.md', '5', '功能配置说明'],
  ['06-resources.md', '6', '辅助模块'],
  ['07-guides.md', '7', '补充指引'],
  ['08-maintaining-public-api.md', '8', '维护者指南'],
  ['09-compatibility.md', '9', '兼容性与升级承诺'],
  ['10-quality-and-onboarding.md', '10', '质量保障与接入准入'],
];

// 第 3 章下的模块（挂在 03-core-capabilities 之下）
const modules = [
  ['03-01-facade.md', '3.1', '门面'],
  ['03-02-agent-module.md', '3.2', 'AgentModule'],
  ['03-03-session.md', '3.3', '会话'],
  ['03-04-routing.md', '3.4', '路由'],
  ['03-05-scheduled-task.md', '3.5', '定时任务'],
  ['03-06-deploy-agent.md', '3.6', '部署代理'],
  ['03-07-paths.md', '3.7', '路径'],
  ['03-08-logger.md', '3.8', '日志'],
  ['03-09-database.md', '3.9', '数据库'],
  ['03-10-attachment.md', '3.10', '附件'],
  ['03-11-agent-settings.md', '3.11', '配置存储'],
];

const START = '<!-- NAV:START (auto-generated by _gen-nav.mjs, do not edit) -->';
const END = '<!-- NAV:END -->';
const esc = (s) => s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

// 折叠全局目录；当前文件项加粗不带链接
function toc(cur) {
  const top_ = ([f, n, t]) => (f === cur ? `**${n}. ${t}**` : `[${n}. ${t}](./${f})`);
  const mod = ([f, n, t]) => (f === cur ? `**${n} ${t}**` : `[${n} ${t}](./${f})`);
  const rows = [];
  for (const it of top) {
    rows.push(`- ${top_(it)}`);
    if (it[0] === '03-core-capabilities.md') rows.push('  - ' + modules.map(mod).join(' · '));
  }
  return ['<details>', '<summary>📖 展开完整文档导航</summary>', '', rows.join('\n'), '', '</details>'].join('\n');
}

// 面包屑
function crumb(cur) {
  const home = '[首页](./README.md)';
  const m = modules.find((x) => x[0] === cur);
  if (m) return `📍 ${home} / [3. 核心能力详解](./03-core-capabilities.md) / **${m[1]} ${m[2]}**`;
  const t = top.find((x) => x[0] === cur);
  return `📍 ${home} / **${t[1]}. ${t[2]}**`;
}

const block = (cur) => [START, '', crumb(cur), '', toc(cur), '', END].join('\n');

function inject(file) {
  const path = join(DIR, file);
  let txt = readFileSync(path, 'utf8');
  const nav = block(file);
  if (txt.includes(START)) {
    txt = txt.replace(new RegExp(esc(START) + '[\\s\\S]*?' + esc(END)), nav);
  } else {
    txt = txt.replace(/^(# .+\r?\n)/, `$1\n${nav}\n`);
  }
  writeFileSync(path, txt);
}

const files = [...top, ...modules].map((x) => x[0]);
files.forEach(inject);
console.log(`✅ 已更新 ${files.length} 个文件的导航块`);
