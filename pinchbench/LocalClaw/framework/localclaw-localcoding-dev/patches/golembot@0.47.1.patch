diff --git a/dist/channels/dingtalk.js b/dist/channels/dingtalk.js
index 669af9102d5adca4b5d956bc8cde597b13e65d11..5d8f3496d3942dca7bf02f4fba8a459a8930ddeb 100644
--- a/dist/channels/dingtalk.js
+++ b/dist/channels/dingtalk.js
@@ -44,24 +44,17 @@ export class DingtalkAdapter {
                 text = data.text?.content?.trim() || '';
             }
             else if (msgtype === 'picture') {
-                // DingTalk picture messages include a download URL
-                const picURL = data.content?.downloadCode || data.content?.picURL;
-                if (picURL) {
-                    try {
-                        const accessToken = await this.dwClient?.getAccessToken?.();
-                        const headers = {};
-                        if (accessToken)
-                            headers['x-acs-dingtalk-access-token'] = accessToken;
-                        const resp = await fetch(picURL, { headers });
-                        if (resp.ok) {
-                            const buf = Buffer.from(await resp.arrayBuffer());
-                            const ct = resp.headers.get('content-type') || 'image/jpeg';
-                            images.push({ mimeType: ct.split(';')[0], data: buf });
-                        }
-                    }
-                    catch (e) {
-                        console.error('[dingtalk] Failed to download image:', e.message);
-                    }
+                // Stream 模式下 picture 消息给的是 downloadCode（非直接 URL），
+                // 需先调 messageFiles/download 换取真实 downloadUrl 再下载。
+                const code = data.content?.downloadCode;
+                const directUrl = data.content?.picURL;
+                try {
+                    const img = await this.downloadByCode(code, directUrl, data.robotCode || data.chatbotUserId);
+                    if (img)
+                        images.push(img);
+                }
+                catch (e) {
+                    console.error('[dingtalk] Failed to download image:', e.message);
                 }
                 text = '(image)';
             }
@@ -73,18 +66,10 @@ export class DingtalkAdapter {
                         if (section.text)
                             text += section.text;
                         if (section.downloadCode || section.picURL) {
-                            const picURL = section.downloadCode || section.picURL;
                             try {
-                                const accessToken = await this.dwClient?.getAccessToken?.();
-                                const headers = {};
-                                if (accessToken)
-                                    headers['x-acs-dingtalk-access-token'] = accessToken;
-                                const resp = await fetch(picURL, { headers });
-                                if (resp.ok) {
-                                    const buf = Buffer.from(await resp.arrayBuffer());
-                                    const ct = resp.headers.get('content-type') || 'image/jpeg';
-                                    images.push({ mimeType: ct.split(';')[0], data: buf });
-                                }
+                                const img = await this.downloadByCode(section.downloadCode, section.picURL, data.robotCode || data.chatbotUserId);
+                                if (img)
+                                    images.push(img);
                             }
                             catch (e) {
                                 console.error('[dingtalk] Failed to download rich text image:', e.message);
@@ -122,6 +107,38 @@ export class DingtalkAdapter {
         await this.dwClient.connect();
         console.log(`[dingtalk] Stream connection established`);
     }
+    /**
+     * 下载钉钉图片。Stream 模式下消息携带 downloadCode（非 URL），需先调
+     * messageFiles/download 接口换取真实 downloadUrl 再 GET。
+     * 若直接给了 picURL（旧/兼容形态）则直接下载。
+     * 返回 { mimeType, data } 或 null。
+     */
+    async downloadByCode(downloadCode, directUrl, robotCode) {
+        let url = directUrl;
+        const accessToken = await this.dwClient?.getAccessToken?.();
+        if (downloadCode) {
+            const headers = { 'Content-Type': 'application/json' };
+            if (accessToken)
+                headers['x-acs-dingtalk-access-token'] = accessToken;
+            const resp = await fetch('https://api.dingtalk.com/v1.0/robot/messageFiles/download', {
+                method: 'POST',
+                headers,
+                body: JSON.stringify({ downloadCode, robotCode }),
+            });
+            if (!resp.ok)
+                throw new Error(`messageFiles/download HTTP ${resp.status}`);
+            const json = await resp.json();
+            url = json.downloadUrl;
+        }
+        if (!url)
+            return null;
+        const imgResp = await fetch(url);
+        if (!imgResp.ok)
+            throw new Error(`image fetch HTTP ${imgResp.status}`);
+        const buf = Buffer.from(await imgResp.arrayBuffer());
+        const ct = imgResp.headers.get('content-type') || 'image/jpeg';
+        return { mimeType: ct.split(';')[0], data: buf };
+    }
     async reply(msg, text, _options) {
         const raw = msg.raw;
         const webhook = raw?._sessionWebhook;
@@ -142,6 +159,24 @@ export class DingtalkAdapter {
             body: JSON.stringify(body),
         });
     }
+    /**
+     * Send a status message. DingTalk can only push new messages via the
+     * sessionWebhook (no edit/delete), so we suppress status updates entirely
+     * to avoid leaving "⏳ thinking..." placeholders in the chat.
+     */
+    async sendStatus(_msg, _text) {
+        return `noop-${Date.now()}`;
+    }
+    /**
+     * Update status — no-op for DingTalk.
+     */
+    async updateStatus(_msg, _statusId, _text) {
+    }
+    /**
+     * Clear status — no-op for DingTalk.
+     */
+    async clearStatus(_msg, _statusId) {
+    }
     async send(chatId, text) {
         const accessToken = await this.dwClient?.getAccessToken?.();
         if (!accessToken)
@@ -160,7 +195,15 @@ export class DingtalkAdapter {
         });
     }
     async stop() {
-        this.dwClient = null;
+        if (this.dwClient) {
+            try {
+                this.dwClient.disconnect?.();
+            }
+            catch {
+                // best effort: ensure the stream socket is torn down
+            }
+            this.dwClient = null;
+        }
     }
 }
 //# sourceMappingURL=dingtalk.js.map
\ No newline at end of file
diff --git a/dist/channels/wecom.js b/dist/channels/wecom.js
index 2bb1d93d621d7b2fb5da3c5634154cf6eb7e264d..32a5c22b48bb7a8b3d24f9cbbd1a0664d216378e 100644
--- a/dist/channels/wecom.js
+++ b/dist/channels/wecom.js
@@ -22,6 +22,9 @@ export class WecomAdapter {
         if (!WSClient) {
             throw new Error('Invalid @wecom/aibot-node-sdk: WSClient not found');
         }
+        // decryptFile (AES-256-CBC) for downloading received images. Optional —
+        // if unavailable, image messages still arrive as '(image)' text.
+        this.decryptFile = AiBot.decryptFile || AiBot.default?.decryptFile || null;
         const wsOpts = {
             botId: this.config.botId,
             secret: this.config.secret,
@@ -30,15 +33,43 @@ export class WecomAdapter {
             wsOpts.url = this.config.websocketUrl;
         this.wsClient = new WSClient(wsOpts);
         this.wsClient.on('message.text', (frame) => {
-            this.handleFrame(frame, onMessage);
+            void this.handleFrame(frame, onMessage);
         });
         this.wsClient.on('message.image', (frame) => {
-            this.handleFrame(frame, onMessage, '(image)');
+            void this.handleFrame(frame, onMessage, '(image)');
+        });
+        this.wsClient.on('message.mixed', (frame) => {
+            void this.handleFrame(frame, onMessage, '(image)');
         });
         await this.wsClient.connect();
         console.log('[wecom] WebSocket connection established');
     }
-    handleFrame(frame, onMessage, fallbackText) {
+    /**
+     * Download and decrypt a WeCom image.
+     * The url is an encrypted, 5-min-valid CDN link; aeskey decrypts it (AES-256-CBC).
+     * Returns { mimeType, data } or null.
+     */
+    async downloadWecomImage(image) {
+        const url = image?.url;
+        const aeskey = image?.aeskey;
+        if (!url)
+            return null;
+        const resp = await fetch(url);
+        if (!resp.ok)
+            throw new Error(`HTTP ${resp.status}`);
+        let data = Buffer.from(await resp.arrayBuffer());
+        if (data.length === 0)
+            return null;
+        // Decrypt if an aeskey is provided (long-connection mode always provides one).
+        if (aeskey && this.decryptFile) {
+            data = this.decryptFile(data, aeskey);
+        }
+        const isPng = data[0] === 0x89 && data[1] === 0x50;
+        const isGif = data[0] === 0x47 && data[1] === 0x49;
+        const mimeType = isPng ? 'image/png' : isGif ? 'image/gif' : 'image/jpeg';
+        return { mimeType, data };
+    }
+    async handleFrame(frame, onMessage, fallbackText) {
         const body = frame?.body ?? frame;
         const msgId = body.msgid || body.msgId || body.message_id;
         if (msgId) {
@@ -50,12 +81,42 @@ export class WecomAdapter {
                 this.seenMsgIds = new Set(entries.slice(entries.length >> 1));
             }
         }
-        const text = body.text?.content ||
+        // Collect images from a standalone image message or a mixed (图文混排) message.
+        const images = [];
+        const imageContents = [];
+        if (body.image)
+            imageContents.push(body.image);
+        if (body.mixed?.msg_item && Array.isArray(body.mixed.msg_item)) {
+            for (const item of body.mixed.msg_item) {
+                if (item?.msgtype === 'image' && item.image)
+                    imageContents.push(item.image);
+            }
+        }
+        for (const ic of imageContents) {
+            try {
+                const img = await this.downloadWecomImage(ic);
+                if (img)
+                    images.push(img);
+            }
+            catch (e) {
+                console.error('[wecom] Failed to download image:', e.message);
+            }
+        }
+        // Text: plain text, mixed text items, or fallback.
+        let text = body.text?.content ||
             body.content?.text ||
             (typeof body.text === 'string' ? body.text : undefined) ||
-            fallbackText ||
             '';
+        if (!text && body.mixed?.msg_item && Array.isArray(body.mixed.msg_item)) {
+            text = body.mixed.msg_item
+                .filter((it) => it?.msgtype === 'text' && it.text?.content)
+                .map((it) => it.text.content)
+                .join(' ')
+                .trim();
+        }
         if (!text)
+            text = (images.length > 0 ? '(image)' : fallbackText) || '';
+        if (!text && images.length === 0)
             return;
         const senderId = body.from?.userid || body.userId || (typeof body.from === 'string' ? body.from : '') || '';
         const chatType = body.chattype || body.chatType || body.chat_type;
@@ -69,6 +130,7 @@ export class WecomAdapter {
             chatType: isGroup ? 'group' : 'dm',
             text,
             messageId: msgId,
+            images: images.length > 0 ? images : undefined,
             mentioned: body.mentioned,
             raw: frame,
         };
@@ -81,6 +143,27 @@ export class WecomAdapter {
         const streamId = `reply-${Date.now()}`;
         await this.wsClient.replyStream(frame, streamId, text, true);
     }
+
+    /**
+     * Send a status message. WeCom doesn't support editing/deleting messages,
+     * so we suppress status updates entirely to avoid polluting the chat.
+     */
+    async sendStatus(_msg, _text) {
+        return `noop-${Date.now()}`;
+    }
+
+    /**
+     * Update status — no-op for WeCom.
+     */
+    async updateStatus(_msg, _statusId, _text) {
+    }
+
+    /**
+     * Clear status — no-op for WeCom.
+     */
+    async clearStatus(_msg, _statusId) {
+    }
+
     async send(chatId, text) {
         if (!this.wsClient)
             return;
diff --git a/dist/gateway.js b/dist/gateway.js
index 81aaf52633406621a347a94d5e152acecdcffbde..b89f172b68b1895b06a0cd7c8b6b052a75ae74ff 100644
--- a/dist/gateway.js
+++ b/dist/gateway.js
@@ -498,10 +498,18 @@ peers) {
                     if (parts.length > 1) {
                         // Everything except the last part is complete paragraphs — send them
                         const complete = parts.slice(0, -1).join('\n\n');
-                        buffer = parts[parts.length - 1];
-                        chunkIdx++;
-                        console.log(`[stream-debug] chunk #${chunkIdx} (paragraph): "${complete.trim().slice(0, 80)}…" [${complete.length} chars]`);
-                        await sendChunk(complete);
+                        // Code-fence guard: if `complete` contains an odd number of ``` fences,
+                        // a fenced code block spans the paragraph boundary and is still open.
+                        // Flushing now would emit a bubble with an unclosed ``` (broken render,
+                        // esp. on WeChat where each reply is a separate immutable message).
+                        // Defer the flush until the fence closes on a later \n\n.
+                        const fenceCount = (complete.match(/```/g) || []).length;
+                        if (fenceCount % 2 === 0) {
+                            buffer = parts[parts.length - 1];
+                            chunkIdx++;
+                            console.log(`[stream-debug] chunk #${chunkIdx} (paragraph): "${complete.trim().slice(0, 80)}…" [${complete.length} chars]`);
+                            await sendChunk(complete);
+                        }
                     }
                 }
                 else if (event.type === 'tool_call') {
diff --git a/package.json b/package.json
index 76e63ef06e8600aac529e64eb0cbed26111d665f..ea4bbd15ec904138f2fa7e3dfc664bb97674e147 100644
--- a/package.json
+++ b/package.json
@@ -9,6 +9,12 @@
     ".": {
       "import": "./dist/index.js",
       "types": "./dist/index.d.ts"
+    },
+    "./dist/*": {
+      "import": "./dist/*",
+      "require": "./dist/*",
+      "default": "./dist/*",
+      "types": "./dist/*"
     }
   },
   "bin": {
diff --git a/dist/channels/weixin.js b/dist/channels/weixin.js
--- a/dist/channels/weixin.js
+++ b/dist/channels/weixin.js
@@ -126,6 +126,12 @@
                     throw new Error(`HTTP ${resp.status}: ${resp.statusText}`);
                 }
                 const data = (await resp.json());
+                // iLink session timeout: errcode -14 = bot_token valid but long-poll session stale
+                if (typeof data.errcode === "number" && data.errcode !== 0) {
+                    console.error(`[weixin] iLink error ${data.errcode}: ${data.errmsg || ""}. Stopping poll, please re-scan QR.`);
+                    this.running = false;
+                    return;
+                }
                 if (data.get_updates_buf) {
                     this.syncBuffer = data.get_updates_buf;
                 }
