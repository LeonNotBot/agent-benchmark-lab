## API Report File for "@lenovo/agent-sdk-channel"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { ChannelAdapter } from 'golembot';
import type { ChannelConfig } from '@lenovo/agent-protocol';
import type { ChannelStatus } from '@lenovo/agent-protocol';
import type { ChannelType } from '@lenovo/agent-protocol';
import type Database from 'better-sqlite3';
import { EndpointRegistryService } from '@lenovo/agent-sdk';
import { EventEmitter } from 'events';
import { IRoutingService } from '@lenovo/agent-sdk';
import { OnModuleDestroy } from '@nestjs/common';
import { OnModuleInit } from '@nestjs/common';
import { RunnerService } from '@lenovo/agent-sdk';
import { SessionService } from '@lenovo/agent-sdk';

// @public (undocumented)
export type AdapterFactory = (channel: ChannelConfig) => ChannelAdapter | null;

// @public (undocumented)
export class ChannelGatewayBridge extends EventEmitter {
    emitChannelSaved(channel: ChannelConfig): void;
    // (undocumented)
    emitChannelStatus(channelId: string, status: string, error?: string): void;
    emitMessagesRead(payload: {
        messageIds?: string[];
        channelId?: string;
        chatId?: string;
        count: number;
    }): void;
    emitNewMessage(msg: MessageRecord): void;
    // (undocumented)
    emitQrDismiss(): void;
    // (undocumented)
    emitQrReady(dataUrl?: string): void;
    // (undocumented)
    emitQrWarning(message: string): void;
    emitSessionUpdate(session: {
        id: string;
        title?: string;
        status?: string;
        cwd?: string;
        kind?: string;
        channelId?: string;
    }): void;
    emitStreamMessage(sessionId: string, message: unknown): void;
}

// @public
export class ChannelModule {
}

// @public (undocumented)
export class ChannelService implements OnModuleInit, OnModuleDestroy {
    // Warning: (ae-forgotten-export) The symbol "NetworkMonitorService" needs to be exported by the entry point index.d.ts
    constructor(db: Database.Database, golemManager: GolemChannelManager, bridge: ChannelGatewayBridge, monitor: NetworkMonitorService);
    clearWechatToken(id: string): ChannelConfig | null;
    // (undocumented)
    deleteChannel(id: string): boolean;
    // (undocumented)
    getChannel(id: string): ChannelConfig | null;
    // (undocumented)
    listChannels(): ChannelConfig[];
    listChannelSessions(channelId: string): Array<{
        id: string;
        title: string;
        status: string;
        cwd?: string;
        kind: string;
        createdAt: number;
        updatedAt: number;
        chatId: string;
    }>;
    // (undocumented)
    migrate(): {
        updated: number;
    };
    // (undocumented)
    onModuleDestroy(): void;
    // (undocumented)
    onModuleInit(): void;
    // (undocumented)
    restartChannelService(id: string): Promise<{
        ok: boolean;
        error?: string;
        engine: string;
    }>;
    // (undocumented)
    saveChannel(data: Partial<ChannelConfig> & {
        type: ChannelType;
    }): Promise<ChannelConfig>;
    // (undocumented)
    testConnection(id: string): Promise<{
        ok: boolean;
        error?: string;
    }>;
    // (undocumented)
    toggleChannel(id: string, enabled: boolean): Promise<ChannelConfig | null>;
    // (undocumented)
    updateStatus(id: string, status: ChannelStatus, errorMessage?: string): void;
    // (undocumented)
    verifyCredentials(channel: ChannelConfig): Promise<{
        ok: boolean;
        error?: string;
    }>;
}

// @public (undocumented)
export type ChatSession = {
    chatId: string;
    channelId: string;
    workspaceDir: string;
    sessionKey: string | null;
};

// @public (undocumented)
export class ChatSessionService {
    constructor(db: Database.Database);
    // (undocumented)
    bind(chatId: string, channelId: string, workspaceDir: string): void;
    // (undocumented)
    resolve(chatId: string, channelId: string): ChatSession | null;
    // (undocumented)
    setSessionKey(chatId: string, channelId: string, sessionKey: string): void;
}

// @public (undocumented)
export class GolemChannelManager {
    constructor(runner: RunnerService, sessions: SessionService, chatSessions: ChatSessionService, messageRecord: MessageRecordService, bridge: ChannelGatewayBridge, factory: AdapterFactory, monitor: NetworkMonitorService, routing: IRoutingService, db: Database.Database);
    getAllFeishuAdapters(): ChannelAdapter[];
    // (undocumented)
    isRunning(channelId: string): boolean;
    // (undocumented)
    restartChannel(channel: ChannelConfig): Promise<void>;
    // (undocumented)
    startChannel(channel: ChannelConfig): Promise<void>;
    // (undocumented)
    stopAll(): Promise<void>;
    // (undocumented)
    stopChannel(channelId: string): Promise<void>;
    verifyWecomConnection(channel: ChannelConfig): Promise<{
        ok: boolean;
        error?: string;
    }>;
}

// @public (undocumented)
export type IncomingRecordInput = {
    channelId: string;
    channelType: string;
    chatId: string;
    senderId: string;
    content: string;
    engine: MessageEngine;
    images?: {
        mimeType: string;
        base64?: string;
        data?: unknown;
        name?: string;
        fileName?: string;
    }[];
    files?: {
        fileName: string;
    }[];
};

// @public (undocumented)
export type MarkReadFilter = {
    messageIds?: string[];
    channelId?: string;
    chatId?: string;
};

// @public (undocumented)
export type MessageDirection = "incoming" | "outgoing";

// @public (undocumented)
export type MessageEngine = "golembot" | "legacy";

// @public (undocumented)
export type MessageQueryFilter = {
    channelId?: string;
    chatId?: string;
    senderId?: string;
    channelType?: string;
    direction?: MessageDirection;
    status?: MessageStatus;
    startTime?: number;
    endTime?: number;
    limit?: number;
    offset?: number;
};

// @public (undocumented)
export type MessageRecord = {
    id: string;
    channelId: string;
    channelType: string;
    chatId: string;
    senderId: string;
    direction: MessageDirection;
    messageType: MessageType;
    content: string;
    hasAttachment: boolean;
    attachmentInfo: string | null;
    status: MessageStatus;
    engine: MessageEngine;
    createdAt: number;
};

// @public (undocumented)
export class MessageRecordService {
    constructor(db: Database.Database);
    deleteOldMessages(retentionDays: number): number;
    getUnreadCount(channelId?: string, chatId?: string): number;
    markAsRead(filter: MarkReadFilter): number;
    queryMessages(filter: MessageQueryFilter): {
        messages: MessageRecord[];
        total: number;
    };
    recordIncoming(input: IncomingRecordInput): MessageRecord;
    recordOutgoing(input: OutgoingRecordInput): MessageRecord;
}

// @public (undocumented)
export type MessageStatus = "unread" | "read";

// @public (undocumented)
export type MessageType = "text" | "image" | "file" | "mixed";

// @public
export function migrateChannels(db: Database.Database): {
    updated: number;
};

// @public (undocumented)
export type OutgoingRecordInput = {
    channelId: string;
    channelType: string;
    chatId: string;
    senderId: string;
    content: string;
    engine: MessageEngine;
};

// @public
export function runChannelMigrations(db: Database.Database): void;

// @public (undocumented)
export class WeChatService {
    constructor(channelService: ChannelService, bridge: ChannelGatewayBridge, golemManager: GolemChannelManager);
    // (undocumented)
    cancelLogin(): void;
    // (undocumented)
    getLoginStatus(channelId?: string): {
        loggedIn: boolean;
        inProgress: boolean;
    };
    isLoggedIn(channelId?: string): boolean;
    // (undocumented)
    startLogin(channelId: string): Promise<{
        qrDataUrl: string;
        deadline: number;
    }>;
}

// (No @packageDocumentation comment for this package)

```
